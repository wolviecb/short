apiVersion: v1
kind: Service
metadata:
  name: {{ include "short.fullname" . }}
  labels:
    {{- include "short.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "short.selectorLabels" . | nindent 4 }}
