apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "short.fullname" . }}-test-connection"
  labels:
    {{- include "short.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "short.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
